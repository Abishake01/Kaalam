name: kaalam
publish_to: 'none'
description: Smart health-aware alarm assistant
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  device_calendar: ^4.0.0
  flutter_local_notifications: ^17.1.2
  permission_handler: ^11.3.1
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  timezone: ^0.9.2

# Optional: add one of these for full offline voice support
# picovoice_flutter: ^3.0.0
# vosk_flutter: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
